@charset "UTF-8";
/* 하위 메뉴 컨테이너 */
.subMenuContainer {
  position: absolute;
  top: 100%; /* 헤더 아래로 배치 */
  left: 0;
  width: 100%; /* 헤더 전체 폭 */
  background-color: white; /* 하위 메뉴 배경 흰색 */
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
  padding: 10px 0;
  overflow: hidden; /* 애니메이션 시 깜빡임 방지 */
  height: 0; /* 기본적으로 숨김 */
  transition: height 0.4s ease, opacity 0.4s ease; /* 애니메이션 부드럽게 조정 */
}

/* 하위 메뉴 스타일 */
.subMenu {
  list-style: none;
  display: flex; /* 가로 정렬 */
  justify-content: center; /* 중앙 정렬 */
  margin: 0;
  padding: 0;
  opacity: 0; /* 기본적으로 숨김 */
  transition: opacity 0.3s ease-in-out;
}

/* 활성화된 하위 메뉴 */
.subMenu.active {
  opacity: 1; /* 메뉴가 보이도록 설정 */
}


