@charset "UTF-8";

/* 전체 페이지 스타일 */
.shopPage {
    width: 90%;
    max-width: 1200px;
    margin: 0 auto;
}

/* 헤더 스타일 */
.shopHeader {
    font-size: 36px;
    font-weight: bold;
    text-align: center;
    margin-bottom: 30px;
    color: #333;
}

/* 검색 창 스타일 */
.searchBar {
    display: flex;
    justify-content: flex-end;
    align-items: center;
    margin-bottom: 20px;
    gap: 10px;
}

.searchBar select,
.searchBar input,
.searchBar button {
    height: 40px;
    padding: 5px 10px;
    border: 1px solid #ccc;
    border-radius: 5px;
    font-size: 16px;
}

.searchBar input {
    flex: 1;
}

.searchBar button {
    background-color: #007bff;
    color: white;
    cursor: pointer;
    border: none;
    border-radius: 5px;
    transition: background-color 0.3s ease;
}

.searchBar button:hover {
    background-color: #0056b3;
}

/* 상품 목록 컨테이너 */
.itemsContainer {
    display: grid;
    grid-template-columns: repeat(3, 1fr); /* 3열로 고정 */
    gap: 20px; /* 각 상품 간의 간격 */
    justify-items: center; /* 가운데 정렬 */
    align-items: start; /* 세로 방향 시작 정렬 */
    margin: 20px auto;
}

/* 상품 항목 */
.productItem {
    width: 300px; /* 고정된 너비 */
    height: 420px; /* 고정된 높이 */
    border: 3px solid #ddd; /* 테두리를 더 굵게 설정 */
    border-radius: 8px;
    overflow: hidden;
    background-color: #fff;
    text-align: center;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
}

.productItem:hover {
    transform: translateY(-5px);
    box-shadow: 0 6px 10px rgba(0, 0, 0, 0.2);
}

/* 상품 이미지 */
.productImg {
    width: 100%;
    height: 200px; /* 고정된 이미지 높이 */
    object-fit: cover;
    background-color: #f5f5f5; /* 이미지가 없는 경우 배경색 */
    border-bottom: 3px solid #ddd; /* 이미지와 정보 영역 구분 */
}

/* 상품 정보 */
.productInfo {
    padding: 15px;
    font-size: 16px;
    color: #333;
    display: flex;
    flex-direction: column;
    justify-content: center;
    height: 150px; /* 정보 영역 고정 높이 */
}

.productInfo h2 {
    font-size: 20px;
    margin: 10px 0;
    color: #007bff;
    text-overflow: ellipsis;
    white-space: nowrap;
    overflow: hidden;
}

.productInfo p {
    font-size: 18px;
    font-weight: bold;
    color: #000;
}

/* 페이지 네비게이션 */
.pagination {
    display: flex;
    justify-content: center;
    align-items: center;
    margin-top: 20px;
    margin-bottom: 50px;
}

.pagination ul {
    list-style-type: none;
    display: flex;
    text-align: center;
    padding: 0;
    margin: 0;
}

.pagination ul li {
    width: 40px;
    height: 40px;
    line-height: 40px;
    font-size: 14px;
    border: 2px solid #ddd;
    margin: 0 5px;
    border-radius: 50%;
    background-color: #f9f9f9;
    cursor: pointer;
    transition: background-color 0.3s ease, color 0.3s ease;
}

.pagination ul li:hover {
    background-color: #007bff;
    color: #fff;
}

.pagination ul li.active {
    background-color: #007bff;
    color: #fff;
}

/* 글쓰기 버튼 */
#writeButton {
    background-color: #007bff;
    color: white;
    padding: 10px 20px;
    border: none;
    border-radius: 5px;
    font-size: 16px;
    cursor: pointer;
    transition: background-color 0.3s ease;
    margin-top: 20px;
}

#writeButton:hover {
    background-color: #0056b3;
}
