function toggleAll(checkbox) {
          const checkboxes = document.querySelectorAll('input[name="cartNo"]');
          checkboxes.forEach(cb => cb.checked = checkbox.checked);
      }

      function deleteCartItem(cartNo) {
          if (confirm("정말 삭제하시겠습니까?")) {
              // Ajax 요청으로 삭제 처리
              fetch(`/shopCartDelete.do?no=${cartNo}`, {method: 'POST'}).then(response => response.text()){
           }
          }
      }
	  function totlaPrice(checkbox) {
	            const totalPrice = document.querySelector('.totalPrice');
				fetch(`/shopCartNoSelect.do?`,{}).then
	            totalPrice.value
	        }