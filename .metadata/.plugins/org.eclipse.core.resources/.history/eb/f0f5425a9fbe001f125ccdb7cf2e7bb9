@charset "UTF-8";


.noticePage{
width: 80%;	
margin: 0 auto;
}
article.article2 p.notice {
	font-size: 11px;
	margin: 10px 0px;
}

/* 부모 div (div.all) 설정 */
article.article3 div.all {
	display: flex !important;
	justify-content: space-between; /* 양쪽 끝으로 배치 */
	align-items: center; /* 수직 가운데 정렬 */
	gap: 20px; /* 각 form 사이 간격 */
	width: 100%; /* 너비를 100%로 설정 */
	margin-bottom: 15px;
}

/* 각 form 내부 정렬 */
article.article3 div.all form {
	display: flex; /* form 내부 요소를 수평 정렬 */
	align-items: center; /* 수직 가운데 정렬 */
	gap: 10px; /* 요소 간 간격 */
}

/* form 내부 요소 (select, input, button) 스타일 통일 */
article.article3 div.all form select, section article.article3 div.all form input,
	section article.article3 div.all form button {
	height: 30px; /* 동일한 높이 */
	padding: 5px 10px; /* 내부 여백 */
	box-sizing: border-box; /* 테두리 포함 크기 계산 */
}

/* 버튼 스타일 */
article.article3 div.all form button {
	background-color: rgb(114, 114, 114);
	color: white;
	border: none;
	border-radius: 3px;
	cursor: pointer;
}

article.article4 table {
	border-top: 5px solid rgb(0, 120, 0);
	width: 100%;
	border-collapse: collapse;
}

article.article4 table tr {
	height: 44px;
	border-bottom: 1px solid rgb(239, 239, 239);
}

article.article4 table th {
	background-color: rgb(238, 238, 238);
}

article.article4 table td {
	font-size: 14px;
	text-align: center;
}

article.article5 {
	position: relative;
	display: flex;
	justify-content: center;
	align-items: center;
	margin-top: 20px;
	margin-bottom: 100px;
}

article.article5 ul {
	list-style-type: none;
	display: flex;
	text-align: center;
	padding: 0;
	margin: 0;
}

article.article5 ul li {
	width: 25px;
	height: 25px;
	line-height: 25px; /* 세로 중앙 정렬 */
	font-size: 14px;
}

article.article5 ul li:hover {
	background-color: rgb(114, 114, 114);
	color: white;
	cursor: pointer;
}

article.article5 form button {
	position: absolute;
	right: 0px;
	background-color: rgb(114, 114, 114);
	border-radius: 3px;
	border: none;
	color: white;
}
article.article5 ul li.active{
	background-color: rgb(130, 130, 130);
	color: white;
}

.article3 form input {
	height: 30px; /* 동일한 높이 */
	padding: 5px 10px; /* 내부 여백 */
	box-sizing: border-box; /* 테두리 포함 크기 계산 */
}
.noticeHeader h1 {
    font-size: 36px;
    font-weight: bold;
    text-align: center;
    margin-bottom: 20px;
    color: #333;
    margin-top: 30px;
}
