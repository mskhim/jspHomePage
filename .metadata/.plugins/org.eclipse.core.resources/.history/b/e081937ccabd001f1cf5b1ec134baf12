@charset "UTF-8";

/* 헤더 스타일 */
.headerNav {
  width: 100%;
  height: 90px;
  background: linear-gradient(90deg, rgba(9,170,92,1) 0%, rgba(0,123,255,1) 100%);
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 10px 30px;
  position: relative;
  z-index: 10;
  margin-bottom: 25px;
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
}

/* 메인 메뉴 스타일 */
.mainMenu {
  list-style: none;
  display: flex;
  justify-content: center;
  flex-grow: 1;
  margin: 0;
  padding: 0;
  gap: 60px;
}

.mainMenu > li {
  padding: 15px 25px;
  font-size: 18px;
  font-weight: bold;
  color: white;
  cursor: pointer;
  white-space: nowrap;
  position: relative;
  transition: background-color 0.3s ease, color 0.3s ease;
}

.mainMenu > li:hover {
  background-color: white;
  color: rgb(9, 170, 92);
  border-radius: 5px;
}

/* 로그인/로그아웃 버튼 */
.Button {
  background: white;
  color: rgb(9, 170, 92);
  border: 2px solid white;
  border-radius: 8px;
  padding: 12px 25px;
  font-size: 18px;
  font-weight: bold;
  cursor: pointer;
  transition: background 0.3s, color 0.3s;
}

.Button:hover {
  background: rgb(9, 170, 92);
  color: white;
  border: 2px solid rgb(9, 170, 92);
}

/* 하위 메뉴 컨테이너 */
.subMenuContainer {
  position: absolute;
  top: 100%;
  left: 0;
  width: 100%;
  background: white;
  padding: 15px 0;
  overflow: hidden;
  height: 0;
  transition: height 0.3s ease, opacity 0.3s ease;
  z-index: 1000;
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
  border-radius: 0 0 8px 8px;
}

/* 하위 메뉴 스타일 */
.subMenu {
  list-style: none;
  display: none;
  justify-content: center;
  margin: 0;
  padding: 0;
  opacity: 0;
}

.subMenu.active {
  display: flex;
  opacity: 1;
  transition: opacity 0.3s ease-in-out;
}

.subMenu li {
  padding: 12px 25px;
  font-size: 16px;
  font-weight: bold;
  color: rgb(50, 50, 50);
}

.subMenu li a {
  text-decoration: none;
  color: black;
  transition: color 0.3s;
}

.subMenu li:hover {
  background: rgb(9, 170, 92);
  color: white;
  border-radius: 5px;
}

/* 아이콘 스타일 */
.headerNav i {
  font-size: 60px;
  color: white;
  margin-left: 35px;
  cursor: pointer;
  transition: transform 0.3s ease;
}

.headerNav i:hover {
  transform: scale(1.2);
}

